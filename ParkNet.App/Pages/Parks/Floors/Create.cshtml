@page
@model ParkNet.App.Pages.Parks.Floors.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Criar</h1>

<h4>Andar</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Floor.ParkId" class="control-label"></label>
                <select asp-for="Floor.ParkId" class="form-control" asp-items="ViewBag.ParkId"></select>
            </div>
            <div class="form-group">
                <label asp-for="Floor.Number" class="control-label"></label>
                <input asp-for="Floor.Number" class="form-control" />
                <span asp-validation-for="Floor.Number" class="text-danger"></span>
            </div>
            <form method="post">
                <button type="submit">Submit</button>
            </form>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
