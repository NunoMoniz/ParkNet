using ParkNet.App.Data.Repositories.PaymentsRep;

namespace ParkNet.App.Data;

public class Print
{
    //private static readonly DbContextOptions<ApplicationDbContext> _space;
    //private static readonly DbContextOptions<ApplicationDbContext> _permit;
    //public static void PrintAll()
    //{
    //    var spaceRepo = new SpaceRepository(new ApplicationDbContext(_space));
    //    var permitRepo = new PermitRepository(new ApplicationDbContext(_permit));
    //    var spaces = permitRepo.GetAll().Where(p => p.SpaceId);
    //    Console.WriteLine("Spaces:");
    //    foreach (var space in spaces)
    //    {
    //        Console.WriteLine($"Id: {space.Id}, Name: {space.Name}, Type: {space.Type}, FloorId: {space.FloorId}");
    //    }
    //}
}
